#!/usr/bin/python3

a, b = 0, 1
if a < b:
    print('a ({}) is less than b ({})'.format(a, b)) #format is a method of the string object
else:
    print('a ({}) is not less than b ({})'.format(a, b))

# print(a < b ? "foo" : "bar") - c like conditional print
print("foo" if a < b else "bar") # 